{
  "name": "electron-persist-secure",
  "version": "1.0.0",
  "description": "Optimized storage for electron with use of a secure contextBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "https://github.com/saucesteals/electron-persist-secure",
  "author": "daniel <saucesteals@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.3.0",
    "electron": "^13.1.6",
    "husky": "^7.0.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "clear": "rimraf dist",
    "build": "yarn clear && tsc",
    "start": "yarn build && node ./dist/index.js",
    "test": "ts-node ./test/test.ts",
    "prepare": "husky install",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "conf": "^10.0.1"
  }
}
